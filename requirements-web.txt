# COSMEON Web API Dependencies (lightweight — no PyTorch/GDAL)
# Used for deployment to Render/cloud. Full requirements.txt is for local dev + satellite pipeline.

# Web framework
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# ML predictions (scikit-learn + advanced ensemble, no PyTorch)
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0

# External API calls (Open-Meteo, GloFAS)
requests>=2.31.0

# Configuration & utilities
python-dotenv>=1.0.0
python-dateutil>=2.8.0

# Authentication
PyJWT>=2.8.0

# Logging
structlog>=23.1.0
